---
# This playbook will install the program dependences of vim to let my vim
# configuration work.

  - hosts: local_machine
    connection: local
    user: shawn
    gather_facts: no
    sudo: yes

    roles:
      - programs_vim_needs
