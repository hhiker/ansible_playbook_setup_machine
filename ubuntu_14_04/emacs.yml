---
# This ansible playbook will setup emacs.
# The playbook should be rewritten next time installing emacs.

  - hosts: "{{ hosts }}"
    user: "{{ user }}"
    gather_facts: no
    sudo: yes

    roles:
      - emacs
