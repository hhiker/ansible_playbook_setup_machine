---
# This playbook will install the file dependences of vim to let my vim
# configuration work.

  - hosts: local_machine
    connection: local
    user: shawn
    gather_facts: no

    roles:
      - files_vim_needs
