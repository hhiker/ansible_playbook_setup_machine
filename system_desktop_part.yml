---
# This playbook will install the necessary programs of ubuntu 12.04 that does
# need a deskop environment to run.

  - hosts: local_machine
    connection: local
    user: shawn
    gather_facts: no
    sudo: yes

    roles:
      - system_desktop_part
