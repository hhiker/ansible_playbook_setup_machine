---
# This ansible playbook will install octopress under ~/mine/softwares/octopress

  - hosts: local_machine
    connection: local
    user: shawn
    gather_facts: no
    sudo: yes

    roles:
      - octopress
